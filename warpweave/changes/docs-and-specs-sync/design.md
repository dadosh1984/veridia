## Context

See `proposal.md`. Five doc files and five spec files have drifted from the current codebase state. No code changes — only markdown corrections.

## Decisions

All changes are editorial: fix numbers, remove false claims, add missing commands, remove non-existent flags. No architectural decisions needed.
